##根据spring约定由于配置的规则,会进行{spring.application.name}-{profile}.properties匹配配置文件
spring:
  application:
    name: user-service-standby-1
  cloud:
    config:
      uri: http://localhost:4001
      profile: prod
      label: master